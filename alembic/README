to run the program type python -m src.gui.app in the terminal

tagging a branch

git status
git add src/gui/accounts_tab.py
git commit -m "Sorting by columns in accounts tab"
git tag -a sprint-3.1-account-sorting -m "Sorting working"
git push origin main
git push origin sprint-3.1-account-sorting